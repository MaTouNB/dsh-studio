/** Host loader entry for the desktop plugin-center browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin-center tabs. */
export function apply(): void {}
